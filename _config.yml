title: Horseman
tagline: Jump over the galaxies
author:
  name: Doy
  email: cloned-doy@fakedomain.com

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >
  This is a cave paintings site depicting Doy's modern life solitude.

# Build settings

theme: minima

plugins:
  # - jekyll-feed
  - jekyll-seo-tag

show_excerpts: true